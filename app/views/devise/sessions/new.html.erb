<div class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-one-third ">
        <div class="box">
          <h2 class="title">Log in</h2>

          <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class="field">
              <div class="control">
                <%= f.input :username,
                            required: false,
                            autofocus: true,
                            wrapper: false,
                            input_html: { autocomplete: "username", class: 'input' } %>
              </div>
            </div>
            <div class="field">
              <div class="control">

                <%= f.input :password,
                            required: false,
                            wrapper: false,
                            input_html: { autocomplete: "current-password", class: 'input' } %>
              </div>
            </div>
            <label for="remember_me" class="checkbox">
              <%= f.input :remember_me, as: :boolean, class: 'checkbox' if devise_mapping.rememberable? %>
            </label>
            <div class="field">
              <div class="control">
                <%= f.button :submit, "Log in", class: 'button is-primary' %>
              </div>
            </div>
          <% end  %>

          <%= render "devise/shared/links" %>

        </div>
      </div>
    </div>
  </div>
</div>
